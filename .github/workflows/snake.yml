name: GitHub Contribution Snake

on:
  push:
    branches:
      - main  # Remplace "main" par le nom de ta branche principale

jobs:
  generate-snake:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Generate Snake
      uses: Platane/snk@v2
      with:
        github_user: SamyMechiche  # Remplace par ton nom d'utilisateur GitHub
        svg_out_path: .github/snake/github-contribution-grid-snake.svg  # Chemin vers ton repo

    - name: Commit and push the generated Snake image
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add .github/snake/github-contribution-grid-snake.svg
        git commit -m "Add Snake Image"
        git push
